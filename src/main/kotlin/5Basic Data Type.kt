import java.io.BufferedReader
import java.util.*

fun main() {
    /*val reader = BufferedReader(someStream)
    val scan = Scanner(System.`in`)

    val n = scan.nextLine().trim().toInt()
    var i :Int = 0;
    for(i in i .. n){
        val word =  inputStream.bufferedReader().use(BufferedReader::readText)
        var size :Int = word.length;
        if(size > 10){
            var ch :Char = word[0];
            var ch2 :Char = word[size-1];
            size = size - 2;
            println("$ch$size$ch2")
        }else{
            println(word)
        }
    }*/

}